package com.budgetpartner.APP.mcp;

/*
@Component
public class UsuarioTools {
    @Autowired
    private UsuarioRepository usuarioRepository;

    @Tool(name = "obtenerUsuarioPorId", description = "Obtiene un usuario por su ID.")
    public Object obtenerUsuarioPorId(@ToolParam(description = "Usuario id") Long id) {
        try {
            return usuarioRepository.findById(id)
                    .orElseThrow(() -> new NotFoundException("Usuario no encontrado con id: " + id));
        } catch (Exception e) {
            return "Error al obtener el usuario: " + e.getMessage();
        }
    }
}*/
