package com.budgetpartner.APP.config;

/*
import com.budgetpartner.APP.llm.*;
import org.springframework.ai.tool.ToolCallbackProvider;
import org.springframework.ai.tool.method.MethodToolCallbackProvider;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;


@Configuration
public class LlmToolConfig {

    @Bean
    public ToolCallbackProvider gastoTool(GastoTools gastoTools) {
        return MethodToolCallbackProvider.builder()
                .toolObjects(gastoTools)
                .build();
    }

    @Bean
    public ToolCallbackProvider miembroTool(MiembroTools miembroTools) {
        return MethodToolCallbackProvider.builder()
                .toolObjects(miembroTools)
                .build();
    }

    @Bean
    public ToolCallbackProvider planTool(PlanTools planTools) {
        return MethodToolCallbackProvider.builder()
                .toolObjects(planTools)
                .build();
    }

    @Bean
    public ToolCallbackProvider tareaTool(TareaTools tareaTools) {
        return MethodToolCallbackProvider.builder()
                .toolObjects(tareaTools)
                .build();
    }


    @Bean
    public ToolCallbackProvider organizacionTool(OrganizacionTools organizacionTools) {
        return MethodToolCallbackProvider.builder()
                .toolObjects(organizacionTools)
                .build();
    }


    @Bean
    public ToolCallbackProvider estimacionTool(EstimacionTools estimacionTools) {
        return MethodToolCallbackProvider.builder()
                .toolObjects(estimacionTools)
                .build();
    }


}*/
